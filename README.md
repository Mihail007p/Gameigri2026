# Северный Край — Skyrim-подобная RPG для слабых Android

Игра делается **под конкретный телефон: Tecno Pova Neo 3** (Helio G85, Mali-G52 MC2,
720×1640 90 Гц, 4/8 ГБ, AnTuTu ~250–320k). Это бюджетное железо, поэтому весь проект
построен вокруг жёсткого бюджета кадра, а не вокруг «красиво, а там посмотрим».

**Статус:** v0.1 — играбельный вертикальный срез. Открытый мир, день/ночь, бой, враги,
квест, NPC с диалогами, лут, инвентарь, сохранение, автоподстройка качества.

---

## Открыть на телефоне (ссылка, ничего ставить не нужно)

Игра лежит прямо в репозитории и раздаётся через CDN jsDelivr. **Рекомендуется Chrome**,
телефон повернуть горизонтально.

**Короткая ссылка (всегда свежая версия ветки):**

```
https://cdn.jsdelivr.net/gh/Mihail007p/Gameigri2026@arena/01a098e5-gameigri2026/play.html
```

**Ссылка на конкретный коммит (не меняется, удобно для сравнения «до/после»):**

```
https://cdn.jsdelivr.net/gh/Mihail007p/Gameigri2026@<SHA-коммита>/play.html
```

> Важно про кэш: jsDelivr кэширует ссылку на ветку до 12 часов. После каждого пуша
> кэш сбрасывается запросом `https://purge.jsdelivr.net/gh/Mihail007p/Gameigri2026@arena/01a098e5-gameigri2026/<файл>`.
> Если видишь старую версию — открой ссылку на конкретный коммит (SHA берётся из `git log`).

Запасные CDN (тот же контент, если первый не открывается):

```
# statically.io — без страницы-предупреждения
https://cdn.statically.io/gh/Mihail007p/Gameigri2026/arena/01a098e5-gameigri2026/prototype/index.html

# raw.githack — показывает предупреждение, нужен один клик «Open the page»
https://raw.githack.com/Mihail007p/Gameigri2026/arena/01a098e5-gameigri2026/prototype/index.html
```

Если вместо игры виден **текст исходного кода** — значит CDN отдал `.html` как
`text/plain`; просто открой ссылку другого CDN. Если игра зависла на
«Генерация мира…» дольше 15 с — на экране появится подсказка с причинами
(сторож в `index.html`), её удобно сфотографировать.

Почему это работает без сервера и без интернета у разработчика: в `prototype/vendor/`
лежит сам three.js (2.1 МБ), поэтому игре не нужны ни npm, ни сборщик, ни сторонние CDN
с библиотеками — только статическая раздача файлов.

## Запуск локально (30 секунд, без сборки)

```bash
npm install     # ставит только three.js (уже продублирован в prototype/vendor)
npm start       # поднимает http://0.0.0.0:3000
```

Дальше открыть адрес в браузере. **На телефоне** — та же ссылка, повернуть горизонтально.
Прототип работает прямо в браузере: никакие Unity/Godot/Android SDK не нужны.

> Если запускаете с компьютера и хотите поиграть на телефоне — телефон и компьютер
> должны быть в одной сети Wi-Fi, ссылка вида `http://<IP-компьютера>:3000`.

## Управление

| Действие | Телефон | Клавиатура/мышь |
|---|---|---|
| Идти / бежать | левый палец: джойстик (до упора = бег) | WASD (+ Shift = бег) |
| Камера | перетаскивание правой половиной экрана | перетаскивание правой половиной |
| Удар | кнопка ⚔ или короткий тап справа | ЛКМ / `R` |
| Блок | удерживать 🛡 | `Q` / ПКМ |
| Прыжок | ▲ | `Space` |
| Взаимодействие | ✋ (появляется у сундуков, NPC, тел) | `E` |
| Сумка | 🎒 | `I` / `Tab` |
| Меню / пауза | ☰ | `Esc` |
| Быстрое сохранение / загрузка | — | `F5` / `F9` |

## Тесты

```bash
node tools/harness.mjs   # 62 проверки игровой логики БЕЗ браузера: рельеф, физика,
                         # ИИ, бой, лут, квесты, сейвы, бюджет draw calls
npm run smoke            # браузерный прогон + скриншоты в tools/shots/ (нужен Chrome)
```

Харнесс — главный инструмент: он гоняет 60 секунд игрового времени автопилотом
и ловит NaN, залипания в рельефе, поломку боя и превышение бюджета.

**Диагностика на телефоне:** в консоли браузера вызвать `__diag()` — вернёт сводку
(FPS, draw calls, треугольники, качество, позиция, GPU). Скриншот этой сводки —
лучший способ показать проблему. На экране также есть постоянно видимая панель FPS
(отключается в настройках).

## Что уже есть в мире

```
   ГОРЫ (стена мира)                                    ~520 × 520 м
   ┌──────────────────────────────────────────────┐
   │ лес              ЛЕДЯНОЙ ХРЕБЕТ              │   • рельеф: процедурный,
   │     ☠ ЗАТОПЛЕННЫЕ РУИНЫ (4 драугра)          │     бесшовный, стриминг чанками
   │  пристань   ~~~~~ ОЗЕРО ~~~~~                 │   • лес: 2100 объектов,
   │             река                             │     инстансинг + отсев по дистанции
   │   ⌂⌂⌂⌂⌂ ДЕРЕВНЯ (Торстен, колодец, костёр)   │   • небо: шейдер, сутки за 10 минут,
   │        лес      🐺 волки                     │     звёзды, луна, туман
   └──────────────────────────────────────────────┘   • день/ночь: свет окон, факелы
```

## Структура кода

```
docs/                     ← читать в первую очередь
  00_VISION.md            концепция и дизайн-столпы
  01_DEVICE_BUDGET.md     БЮДЖЕТ КАДРА под Tecno Pova Neo 3 (главный документ)
  02_ROADMAP.md           этапы и выбор движка для APK
prototype/
  index.html  style.css   разметка и стиль HUD
  vendor/                 three.js r186 (локально, чтобы работало без интернета)
  src/
    main.js               точка входа, игровой цикл, квесты, диалоги, сохранения
    config.js             ВСЕ числа игры: мир, качество, игрок, враги, квесты
    core/  settings.js    пресеты качества + авторегулировка
           perf.js        замер кадра и гувернёр (авто-снижение качества)
           input.js       сенсор + клавиатура + мышь
           sfx.js         процедурный звук (WebAudio, 0 байт ассетов)
           save.js        сохранения
    world/ terrain.js     функция высоты и стриминг чанков
           sky.js         небо, солнце, звёзды, свет, туман, дальние горы
           props.js       деревья/камни/кусты (InstancedMesh + корзины)
           structures.js  деревня и руины (сливаются в 1 меш)
           water.js       вода
    entities/ humanoid.js низкополигональные фигуры и процедурная анимация
              player.js   игрок: движение, камера, бой, прокачка
              enemies.js  ИИ драугров и волков, лут, респавн
              npc.js      жители
              fx.js       частицы (один InstancedMesh на всё)
    ui/    hud.js         HUD, меню, диалоги, инвентарь
    utils/ geo.js         сборка геометрии без моделей
           noise.js       шум и ГСЧ (весь мир — математика, 0 байт ассетов)
tools/ harness.mjs        headless-тест логики
         smoke.mjs        браузерный прогон со скриншотами
server.mjs                статик-сервер
```

## Три принципа, на которых держится производительность

1. **0 байт ассетов.** Весь мир — математика (процедурный рельеф, шум) и примитивы
   (коробки/конусы). Ни текстур, ни моделей, ни аудиофайлов: звук синтезируется.
   Для eMMC 5.1 и APK размером в десятки мегабайт это критично.
2. **Минимум draw calls.** Инстансинг растительности (2100 объектов → 5 вызовов),
   слияние всей деревни и руин в один меш (3128 треугольников → 1 вызов),
   сон врагов дальше 95 м, тени только от ближайших.
3. **Render scale вместо красоты.** Рендерим в 0.55–1.0 от нативного разрешения.
   На Mali-G52 это самый дешёвый способ поднять FPS. Гувернёр сам подбирает
   масштаб под реальную температуру и загрузку чипа.

## Дальше

См. `docs/02_ROADMAP.md`: этап 0 — замер на реальном телефоне, этап 1 — вертикальный
срез на 20–30 минут игры, этап 2 — перенос в Godot 4 (или упаковка в APK через Capacitor).
